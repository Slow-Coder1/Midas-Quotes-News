{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI: Uvicorn",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app.main:app", "--reload", "--port", "8787"],
      "jinja": true,
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
